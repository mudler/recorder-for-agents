# Minimal i3: no bar, no borders, fullscreen the first window so the recorded
# program fills the whole frame.
font pango:monospace 8
default_border none
for_window [class="XTerm"] fullscreen enable
for_window [class=".*"] fullscreen enable
